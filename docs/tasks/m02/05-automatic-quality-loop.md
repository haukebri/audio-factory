# 05 — Reject and regenerate within a durable budget

Status: [x] Complete — bounded automatic loop and controlled recovery/browser checks passed on 2026-09-09

Overview: [M2 queue](00-overview.md) · [Project](../../project-overview.md)
Milestone: [M2 — Review studio and automatic quality control](../../milestone/05-review-and-quality.md)
Depends on: [04 — Connect and preflight LAION Larger CLAP General QA](04-audio-judge.md).
Expected duration: 45–90 minutes.

## Outcome and scope

Extend the shared workflow from task 02 using the real judge from task 04. Apply deterministic validity checks and the versioned Larger CLAP General score/signal policy from task 04 to the delivered candidate. Policy thresholds and reason tags determine the automatic verdict; no LLM response or generated audible observations are required. Keep final intent fixed. For an identified trim problem, evaluate a bounded alternative region/cut before generating again; record every derivative and its verdict. On semantic rejection, choose and persist a new seed and launch another generation only within the three-attempt/20-minute budget. Persist all candidate assets first.

Implement explicit auto_accepted, rejected, needs_review, exhausted, cancelled and operational-error outcomes. Uncertainty or a missing judge must not become a pass or a regeneration storm. Human rejection can request another attempt within the selected budget; explicit additional budget creates a recorded continuation. Human decisions override presentation/export selection without erasing judge history. Studio and CLI/agent access share the same policy and durable IDs.

Maintain cancellation across generation and judge boundaries. On restart reuse a completed candidate; never repeat an uncertain external operation automatically. Show the reason for each new attempt, elapsed/remaining budget and current candidate in the UI. Do not conflate accepted-by-model with approved-by-human.

## Required checks and review demo

- Controlled judge/backend sequences cover reject→accept, repeated rejection→exhaustion, uncertain, missing judge, timeout and cancellation.
- Assert exact generation count and distinct recorded seeds; first candidate and feedback survive every attempt.
- Restart between persisted generation, judging and retry transitions without duplicate computation; reconnect UI to the same request.
- Verify alternate-region evaluation judges the final cut and preserves source hashes. Exported outcome retains correct candidate/judge/human identities.

## Preflight and recovery

Use controlled fixtures for exhaustive transition checks. Reuse task 04's real model capability evidence; task 07 owns the new real end-to-end jobs. No unbounded retries or automatic provider/backend swaps. On persistence failure, stop before another generation; on exhausted budget, retain all candidates for review.

Follow the shared execution, evidence and completion agreement in [00-overview.md](00-overview.md). Implement only this task and its integration needs, update this status/overview plus directly supported milestone items, and leave later tasks to the queue. The runner owns review and commits; do not change the runner or commit from the implementation session.

## Evidence

Implemented against clean input revision `6d58f56487a845dc5e85575276a2c9dc89309eba` on 2026-09-09. Read this task completely, its queue/project overview, milestone specification, tasks 02/04 evidence, supplied working agreements and the workflow/service/backend/QA/store/export/CLI/studio callers. No repository or ancestor AGENTS.md file exists. Only task 05 and its integration/docs changed; no runner changes or commit.

**Preflight and inherited evidence.** Node, pnpm, FFmpeg, signal Python and agent-browser were available; `df -k .` reported 705,834,400 KiB available. All runs used fresh owned fixture roots and ephemeral ports. Real model capability is inherited from task 04: `judge.mjs` SHA-256 `678f953f14cbb0efdf8aad3a124025695655780139550a8c3c5035740cb5f8c9`, policy `7a3460d0778854baa30454e2f67f5d4d5dd335df507cd2c0f332b6155a2ccda5`, model lock `0a8b482d505bb2c7b316f41bc0c10cfdae4d2ea42521346a6bbebec3df9f3d9a` were rehashed and match task 04. Generation configuration and generation/QA/dependency locks also remain unchanged. No model download, provider swap, cloud call or real generation was performed.

**Implementation.** Automatic mode defaults to three attempts/20 minutes after initial setup; explicit selected budgets retain the existing limits. One request records every attempt ID, distinct seed, reason, candidate and verdict. Sources/cuts are verified and immutable before judging or another generation. The existing versioned judge supplies decisions; uncertainty/missing QA stop for review, timeouts and failures remain operational errors, and exhaustion never selects a rejected sound as accepted. A trim reason permits one alternate detected region or a 100-ms expansion on each side, judged against its actual delivered bytes. Manual derivatives are also saved before scoring.

Durable checkpoints reuse completed generation, analysis, cut and judge evidence. Explicit recovery keeps the request ID; unknown in-flight operations are never replayed. Persisted cancellation survives restart. Failed persistence prevents another generation/cleanup. Legacy compute mutations return 429 during workflow QA/judging. Explicit human retries retain the remaining budget; additional budget is an idempotent, recorded parent-linked continuation. Human feedback and export selection remain separate from immutable automatic history. Studio displays outcomes, retry reasons, seeds, elapsed/remaining budget and current candidate without replacing focused controls on clock ticks. `./run workflow <workflow.json> <key>` exposes the same workflow and durable IDs to CLI clients; existing make/generate behavior remains compatible.

**Automated and smoke commands.**

- `pnpm build` passed. `pnpm test:audio-factory > .test-artifacts/m02-quality-suite-final.log 2>&1` passed **20 Node checks and 2 Python checks**, including isolated frozen-install/build, existing service/QA/store/bundle behavior and owned subprocess cleanup.
- After the final setup-reuse change, `node --test quality-loop.test.mjs studio.test.mjs studio-frontend.test.mjs > .test-artifacts/m02-quality-affected-final.log 2>&1` passed **9 checks**. After the final clock/readiness UI change, `node --check studio.js` and `node --test studio-frontend.test.mjs` passed; the changed UI was exercised in the real browser below.
- `quality-loop.test.mjs` runs real signal analysis/FFmpeg/bundle validation with synthetic generation and controlled inputs to the real `decide` policy. It verifies reject→accept (exactly 2 generations), repeated rejection→exhaustion (3), uncertainty (1), and alternate-region acceptance (1 generation, 2 delivered hashes/judgments). Seeds are distinct; first-candidate feedback is written before subsequent generations and survives all attempts, restart and relocation. Export extraction verifies exact source/cut/judge/human identities. A copied isolated CLI reattaches to the same completed request without computation.
- Missing-runtime and real judge-process timeout paths, cancellation during generation and judging, and an accelerated wall-clock deadline stop without regeneration. A forced ENOTDIR persistence failure retains the candidate/feedback and prevents a second generation. Separate authenticated generation/QA submissions during the judge return 429.
- Six controlled child-process exits (`node quality-loop.test.mjs --crash <owned-root> <phase>`, exit 23) cover source, cut, evaluated result, retry transition, judging and alternate-cut boundaries. Each store is relocated and owned `out/` deleted before recovery. Completed work is reused with exact operation counts; unknown judge/alternate-cut operations stop unresolved. Cancellation-state reopening never launches work.

**Actual browser journey.** `node quality-loop.test.mjs --browser` created `.runtime/quality-browser-lgwO4r`, PID 68936, port 49166. Named session `agent-browser --session m02-quality` used `open`, `snapshot -i`, `fill`, `select`, `click`, condition-based `wait --fn`, `reload`, `eval --stdin`, `download` and `screenshot --full`. It submitted automatic mode, observed two attempts under request `a6018136bff92cb855a68dffbe909b2e` (seeds 1561982705/1748451376), verified reasons/budgets/current candidate, and completed native playback. A synthetic rejection was recorded on auto-accepted candidate `0f6f16e8681b2725c725b719f84ffcdcbbbfa0e167ff4c8753366c9a4fd761fa`; both verdicts remained visible. Refresh and SIGUSR2 service restart retained request/selection/feedback. Explicit retry used seed 29429662 under linked request `f6508dbb3ace9292703be26f6db667ae`. The browser then granted one attempt/one minute as continuation `df28efdc63f828e294e36485ba4f9dca` (seed 171760948), with the budget recorded on its parent.

The browser-downloaded `.test-artifacts/m02-quality-browser-export.tar` was extracted into an owned temporary directory and checked using `verifyExport`; the directory was removed. Exported automatic verdict stayed auto_accepted and human verdict stayed rejected, tied to the same candidate and delivered hash `7dbad3b41c68695dc5e5d51b002861a722068f8f3b843e3ec346d3ecef9153ef`. Synthetic source hash: `98e954275f896b2a78cbc43f45d01abf9dc029a263fa72b73d57f47cb17cacaf`. Final full-page screenshot was visually inspected, with no horizontal overflow. A browser assertion verified a focused history button survives clock updates. Mode selection survives refresh. Playback and fixture feedback are technical checks, **not human listening acceptance or quality labels**.

**Findings resolved.** The targeted alternate-cut crash regression initially failed: a previous cut incorrectly cleared the new cut's in-flight marker. Matching the pending cut bounds fixed it; `.test-artifacts/m02-quality-recovery-regression.log` and `m02-quality-recovery-fixed.log` preserve failure/pass evidence. The CLI's new branch initially failed TypeScript's unknown-request check; it now uses the existing validated prompt cast. A browser wait against a pre-reload script timed out; reloading the changed assets and repeating select/reload passed. No failed mandatory check remains unresolved.

**Artifacts, mutations and cleanup.** Exact implementation/artifact hashes are in `.test-artifacts/m02-quality-hashes.json`. The final suite/affected logs, browser evidence/continuation/history/operation records, downloaded TAR and inspected screenshot remain ignored under `.test-artifacts/m02-quality-*`. Every test's generation, candidate, feedback and copied CLI token was confined to owned fixture roots, all removed. Closed the named browser session, sent SIGTERM only to owned PID 68936 and awaited its clean exit; port 49166 is unreachable. `.test-artifacts/m02-quality-cleanup.json` confirms no remaining quality fixture roots and all recorded controlled PIDs absent; process inspection found no model/QA/setup process. Temporary implementation scripts were removed. Existing retained audio, wanted output and caches were preserved. `git diff --check` passed.

**Not run / not claimed.** New real end-to-end jobs and `pnpm audio:smoke` belong to task 07 and were not rerun. No human listening, acoustic calibration or quality-improvement claim is made. Task 04's unchanged real-model capability evidence is inherited. All task-05 machine-verifiable checks are complete; no owner-acceptance gate applies.

Final review-and-fix pass, 2026-09-09 (same input revision; no recovery commits): fixed two demo blockers. Operational judge errors now finish with failed job status, so CLI callers cannot report successful completion after a timeout. Recovery compares QA/cut requests independent of JSON property order, reusing a persisted accepted alternate cut without cutting/judging it again. The timeout-status assertion and alternate-evaluated crash/relocation regression each failed before their respective fix and passed afterward.

`pnpm build` and `node --test quality-loop.test.mjs studio.test.mjs studio-frontend.test.mjs service.test.mjs` passed all 13 checks; `git diff --check` passed. Review logs are `.test-artifacts/m02-quality-review-{timeout-regression,alternate-regression,final}.log`. Fixture roots and controlled children were cleaned up by the checks. Browser assets/server, retained browser screenshot/export and judge/model/policy hashes match the prior evidence above, which is inherited. No real generation, new browser journey, human listening or commit was performed.
