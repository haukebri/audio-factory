# 06 — Use human feedback for repeatable quality evaluation

Status: [x] Complete — local evaluation/collection, cached comparison and rollback verified; quality_not_established (2026-09-09)

Overview: [M2 queue](00-overview.md) · [Project](../../project-overview.md)
Milestone: [M2 — Review studio and automatic quality control](../../milestone/05-review-and-quality.md)
Depends on: [05 — Reject and regenerate within a durable budget](05-automatic-quality-loop.md).
Expected duration: 45–90 minutes.

## Outcome and scope

Export real human feedback and immutable audio/provenance into a versioned local evaluation dataset. Exclude synthetic fixtures, automatic labels and superseded human decisions from ground truth while retaining audit history. Group related prompts/source lineages and duplicate audio into one partition. Establish development/holdout separation before tuning; add a UI view of label counts, coverage, disagreements and unreviewed examples with optional blind review.

Implement repeatable comparison of the recorded M1 signal/CLAP baseline (`laion/clap-htsat-unfused`), the Larger CLAP General score/signal policy and bounded selection policy. Report confusion counts, precision, false accepts/rejects, uncertainty coverage, human approval of selected outcomes, attempts and latency with denominators and uncertainty. Freeze thresholds/rubrics/policies and benchmark manifests; support selecting a tested policy and rollback without rewriting old verdicts. Begin with evaluation-driven description/threshold/region-policy adjustments, not online model training.

Use the milestone's initial targets and minimum human sample. If real labels are insufficient, run the metric machinery on explicitly synthetic fixtures and report quality_not_established with the exact missing counts. Deliver the working evaluation/collection workflow; do not invent human listening or hold the entire engineering queue waiting for labels. Leave milestone quality checkboxes unchecked.

## Required checks and review demo

- A small known synthetic dataset independently verifies confusion counts, abstention handling, deduplication, correction semantics and grouped split isolation.
- Export/import preserves candidate hashes and human label provenance; no auto verdict becomes ground truth.
- Run on available real feedback, report exact counts and either actual measured results or insufficient-data status.
- Demonstrate development-only policy changes, repeatable holdout evaluation and rollback with historical results unchanged.

## Preflight and recovery

No new generations are needed to build evaluation. Reuse collected clips and cached judge outputs when audio/model/rubric hashes match. Full reevaluation needs an explicit recorded workload budget. Never upload the dataset or train on held-out labels. Missing real labels limits the quality conclusion, not the deliverability of this tooling.

Follow the shared execution, evidence and completion agreement in [00-overview.md](00-overview.md). Implement only this task and its integration needs, update this status/overview plus directly supported milestone items, and leave later tasks to the queue. The runner owns review and commits; do not change the runner or commit from the implementation session.

## Evidence

Implemented against clean input revision `f3a745973275aeb8dfdbf0def023fd4dcd925c5e`. Read this task completely, its queue, milestone specification, project overview, predecessor evidence, supplied working agreements and the store/judge/workflow/studio/QA/export callers. No repository or ancestor AGENTS.md file exists. Only task 06 and its integration/checks/docs changed; no commit or runner modification.

**Preflight and scope.** Node 22.22.3, pnpm, FFmpeg, signal Python and agent-browser were available; disk preflight reported 705,835,788 KiB available. No default `.runtime/studio` existed before this task; discovered candidate directories during suite execution belonged to controlled tests. The real collection therefore contains zero candidates/labels. Existing model/runtime/dependency pins and generation behavior are inherited; no models downloaded, real generations, inference, cloud upload or training occurred. The default judge decision math and original default rubric hash remain compatible; selected policies are snapshotted only for new requests.

**Implementation.** [Evaluation workflow](../../evaluation.md) documents exact CLI usage and limitations. `evaluation.mjs` exports a versioned local JSON dataset containing immutable audio bytes, candidate hashes, complete human event provenance and job histories. Import reuses store/schema/lineage validation and publishes only a verified new store. Latest human decisions supply labels; automatic/legacy labels and fixtures never enter real metrics. Exact duplicate audio counts once, conflicting current labels/different intents are withheld, and correction audit history remains intact. Explicit prompt-family maps plus normalized prompts, source hashes, attempts, duplicate audio and connected request/continuation lineages share a deterministic frozen partition. Family maps require review for semantic/near-duplicate relationships; CLAP similarity is not used to invent identity.

Frozen benchmarks retain targets, model locks, implementation hashes and split assignments. Development registration freezes policy versions; holdout inspection prohibits new policy tuning for that benchmark, and previously inspected holdout identities cannot migrate into development through a new registry benchmark. Repeated holdout runs are identical. Explicit selection/rollback requires a tested policy, records the predecessor, and affects new workflow requests/cuts without altering historical jobs, verdicts or results. No policy was selected in the real root.

Cached comparison validates audio/model/revision/artifact/description/target/rubric provenance. M1 was advisory: the frozen, explicitly identified evaluation proxy requires target-first windows plus signal checks, rather than fabricating a historical automatic verdict. Source scores are not reused for different delivered audio. Reports include confusion counts, labelled and total denominators, false accepts/rejects, precision, uncertainty/coverage, Wilson intervals, matched-input comparisons, selected human approval, first-attempt comparison, attempts and recorded generation/QA latency. Missing scores/retries/regions are explicit abstentions or missing evidence. Unrecorded memory is unavailable. Results are exploratory, not established quality.

**Automated and smoke checks.**

- `pnpm build` passed; `node --check studio.js`, `node --check evaluation.mjs` and `git diff --check` passed.
- `pnpm test:audio-factory > .test-artifacts/m02-evaluation-suite-final.log 2>&1` passed **21 Node checks and 2 Python checks**, including isolated frozen-install/build, store/bundle validation, judge behavior, service boundaries, workflow crash/relocation and owned-process cleanup.
- After the final cached replay refinement, `node --test evaluation.test.mjs judge.test.mjs quality-loop.test.mjs studio.test.mjs studio-frontend.test.mjs > .test-artifacts/m02-evaluation-affected-final.log 2>&1` passed **12 checks**.
- Final `EVALUATION_EVIDENCE=.test-artifacts/m02-evaluation-synthetic.json node --test evaluation.test.mjs > .test-artifacts/m02-evaluation-focused-final.log 2>&1` passed. Independently specified fixture totals: Larger CLAP **TP=2, FP=1, FN=1, TN=1, approved abstentions=1, rejected abstentions=1**; M1 proxy **TP=4, FP=3, FN=0, TN=0, abstentions=0**. Eight unique audio clips include seven labelled clips and one automatic-only/unlabelled clip. A ninth candidate duplicates audio and does not increase denominators. Two corrections retain all history and use only the final decision. Conflicting duplicate labels are excluded. Explicit related families, duplicate audio and different cuts sharing a source cannot cross splits.
- The same check verifies changed development thresholds, absent-description cache abstention, a recorded reject→accept sequence (2 attempts, selected exact candidate identity, 6020 ms recorded work, first rejected/selected approved), import byte/hash/event/timestamp equality, deletion of the original store, relocation and a new Node-process reopen. Tampered assets/actors are rejected before import publication. Policy selection→change→rollback leaves old result bytes unchanged; holdout repeat is byte-identical; new holdout-derived tuning is rejected; an existing job retains its original policy snapshot after selection changes.

**Real feedback run.** `node evaluation.mjs export .runtime/studio .test-artifacts/m02-evaluation-real-final.json` created an empty private default store and exported its actual zero rows. `node evaluation.mjs freeze .test-artifacts/m02-evaluation-real-final.json` froze dataset SHA-256 `e19ff7b06f69dbce143b7064ab6858283c8a9a2f0630a5f093a11809ff9786e7` as benchmark `6d01a9c053b8bf232d0e97689dc69c84ac53048563c6537621f93255d11e8ce7`. `node evaluation.mjs register 6d01a9c053b8bf232d0e97689dc69c84ac53048563c6537621f93255d11e8ce7` produced policy `7787ae4ca1ea865954bcfdbc7e3d8ccde538fa28c32cd4ed5f5c2adfe3a79608`. Running `node evaluation.mjs run <benchmark> <policy> holdout` twice with those exact IDs produced identical result `31143e50492891e9fdf0d1bcc60513d04601ae1197e70434d52109761000426d`.

Actual counts: **0 real labelled clips, 0 approved, 0 rejected, 0 families**, in both partitions. Exact missing counts: **100 clips, 50 approved, 50 rejected, 10 prompt families**. Status is **quality_not_established**; all rates with zero denominators are null, not success. Full output is `.test-artifacts/m02-evaluation-real-{counts-final,register,result}.json`; synthetic-only metric artifacts are clearly separated. No human listening or improvement is claimed, and both milestone quality checkboxes remain unchecked.

**Actual browser journey.** `node evaluation.test.mjs --browser` opened owned fixture `.runtime/evaluation-test-MI9tiF`, PID 90331, ephemeral port 51331, with eight explicitly synthetic candidates and no generation-capable executor. Named `agent-browser --session m02-evaluation` used `open`, `snapshot -i`, `check`, `click`, condition-based `wait`, `fill`, `reload`, `eval --stdin`, `download` and a full screenshot. Selected unreviewed synthetic family 7, enabled blind review, completed native audio playback, and saved the explicit note “Synthetic browser check; no human listening claim” with fixture approval. Refresh retained the selection/feedback/blind toggle and kept automatic evidence hidden. The collection panel still reported **0/0 real clips and eight synthetic candidates excluded**, with the exact missing counts. The real-unreviewed action was correctly disabled for this all-fixture collection.

`download` saved `.test-artifacts/m02-evaluation-browser-export.json`; `node evaluation.mjs import .test-artifacts/m02-evaluation-browser-export.json .test-artifacts/m02-evaluation-browser-import` verified all eight candidate/audio/history records. The imported test directory was then removed. `.test-artifacts/m02-evaluation-browser.png` was visually inspected; the panel, labels/buttons and existing listening desk were readable, with no horizontal overflow. Playback and fixture approval are technical checks, not actual human ground truth.

**Findings and recovery.** The initial known-count test found property-order-sensitive policy hashing after the store canonicalized JSON: the same default policy produced `7a3460…` versus `212525…`. Fixed the shared hash function to recognize structural equality and canonicalize new policies while preserving the historical default hash. The first full suite also exposed omitted new runtime files in two isolated-copy fixture inventories; their copy lists now include evaluation and the retained M1 lock. The initial failures remain in `.test-artifacts/m02-evaluation-suite.log`; all final checks pass.

**Mutations, artifacts and cleanup.** Created only owned fixture roots, ignored evidence exports/imports, an empty default studio store and content-addressed evaluation registry records. Early/final benchmark snapshots remain preserved in `.runtime/evaluation`; there is no `active.json` and no real policy activation. All fixture policy selections were confined to owned test roots and removed. Closed the named browser session, sent SIGTERM only to owned PID 90331 and verified process exit, unreachable port 51331, removed fixture root and removed imported test store. `.test-artifacts/m02-evaluation-cleanup.json` records those checks; final affected tests removed their controlled roots/children, and process inspection found no model/QA/setup process. Existing retained audio, output and caches were preserved. `.test-artifacts/m02-evaluation-hashes.json` records exact implementation/artifact hashes; final suite/focused logs and synthetic/real reports remain ignored.

**Not run / not claimed.** New real model inference and `pnpm audio:smoke` belong to task 07 and were not run. Full model rescoring, human listening, acoustic near-duplicate discovery and quality calibration were not performed. Their absence limits the quality conclusion, not the completed collection/evaluation tooling. No owner-acceptance pause applies.

**Final review and fix (2026-09-09).** Reviewed the complete task against starting commit `f3a745973275aeb8dfdbf0def023fd4dcd925c5e`, with no recovery commits or staged changes, and all working/untracked implementation files. Fixed one demo-blocking evidence error: bounded replay previously selected the historical final cut even when a comparison threshold would avoid that alternate region, and charged unused alternate work. Replay now evaluates the first cut, follows the comparison policy's trim reasons to the recorded alternate, and reports missing required alternate evidence as unresolved. One behavioral regression failed on the old selected candidate identity and now verifies both region branches, missing evidence and 3010 ms for first-cut-only work. Logs: `.test-artifacts/task06-review-regression-{before,after}.log`.

`pnpm build`, syntax checks for `studio.js`/`evaluation.mjs`, and `git diff --check` passed. `node --test evaluation.test.mjs judge.test.mjs quality-loop.test.mjs review-store.test.mjs studio.test.mjs studio-frontend.test.mjs` passed **14 checks** after the fix (`.test-artifacts/task06-review-final.log`), including fixture cleanup. Unchanged judge/store/workflow/studio files and browser export/screenshot match the prior evidence hashes; `.test-artifacts/task06-review-hashes.json` records inherited and revised hashes. No browser or inference rerun was needed for this evaluator-only fix.

Reran the actual empty review store under the revised evaluator using a separate `.test-artifacts/task06-review-registry`: benchmark `fdd74db364307dbe36ae56ec39a6b00d10b2fda8c1481f3abb61eeb098e529a4`, policy `7787ae4ca1ea865954bcfdbc7e3d8ccde538fa28c32cd4ed5f5c2adfe3a79608`, repeated identical holdout result `3b1c055c4f8f823bc5afa13d582fcf5cd97616721fa84dfd827a5bbea017f6d7`. `.test-artifacts/task06-review-real.json` retains zero real labels and missing counts of 100 clips, 50 approved, 50 rejected and 10 families, with `quality_not_established`. Only owned test stores and ignored evaluation evidence were created; prior results remain preserved, no active policy was selected, and no commit was made.
