# Test-suite audit (original review and tasks 12/14 cleanup)

Reviewed 2026-09-10, source revision `c5ad0b28871a7a9c0b55433f2b0b334bb7a66bef`, initially clean worktree. [Production-first model](project-model.md) was saved before test sources/baseline. No production or test edits were made. Recommendations below are proposals, not an approved cleanup. Findings each have a numbered `*-test-*.md` task in the parent directory.


## Task 12 cleanup — 2026-09-10

The owner's request to implement task 12 authorized its exact six-scenario removal plan before edits. Applied and verified: `browser/source`, `browser/injected-states`, `browser/semantic-setup`, `browser/pending-request`, `browser/cancel-request`, and `browser/evaluation`, including their local setup/waits and now-unused `runId`. The terminal success message now lists only retained checks. No replacement assertions or product changes were made. The original inventory and counts below remain the historical review baseline: 21 browser groups reconcile to 6 removed and 15 retained, with no new groups.

All 15 retained groups reached their unchanged assertions in `node scripts/studio-ui-browser-check.mjs` (exit 0): fixture-preflight, identity, polling, single-player, shared-drafts, comparison-reload, history-back, review-export, rejection, trim, disconnect, library, invalid-submission, background-complete and completion-reload. The persisted results contain the 14 named behavior records; fixture root/readiness assertions passed before them. The owned `scripts/studio-ui-fixture.mjs` supplied synthetic PCM and prompt preparation; no model or provider calls occurred.

Verification: `pnpm build` passed. The first `pnpm test:audio-factory` run passed 33/34 Node checks; `elevenlabs.test.mjs:58` failed with `Local compute/backend already owned by a live process` (`failed` versus expected `completed`). Python did not run after that failure. An unchanged full-command rerun after browser fixture shutdown passed all 34 Node checks (including frontend, sound grouping and Studio controlled smoke) and both Python signal checks, with no skips. The initial ownership refusal is preserved, not bypassed; the exact competing owner was not captured, so no definitive contention source is claimed. No test or product changes were made to obtain the rerun pass.

Evidence is preserved under `.test-artifacts/feedback-1-task-12/`: `mutation.txt` and `browser-before.mjs` record the applied batch and pre/post fingerprints; `browser.log`, `studio-ui-browser-results.json`, `fixture.log`, `node-python.log`, `node-python-repeat.log` and `cleanup.txt` record execution. Browser session `af-acceptance` closed, fixture PID 43170 exited, port 63085 closed, owned root `.runtime/studio-ui-8vhCnB` was removed, and pre-existing shared manifests were restored. `git diff --check` passed.

For B9, the retained browser groups now have demonstrated execution rather than inspection-only evidence. This does not fill the B3 lost-response or B4 cancellation browser gaps: their old setup never reached those central operations. Removed partial protection includes source-only presentation and synthetic historical state/setup rendering. Current source-only download, primary five-take winner selection, batch deep links, supported duration/paid-limit UI, and current pending/cancel flows remain outside this passing script; task 13 remains open. Semantic evaluation is intentionally removed, not missing supported coverage. No real-generation/paid smoke or human listening acceptance ran or was required by task 12. Other cleanup proposals and historical findings below are unchanged.

## Task 14 cleanup — 2026-09-10

The owner's request to implement task 14 authorized its exact listed removal before edits. At revision `70a5c7126cf3b66f8bfb1b50f704a75002ba60e7` with a clean worktree, removed only `GitRecoveryTests.test_dry_run_reports_but_does_not_commit_dirty_tree`. No narrower replacement, new assertions, helper removal or production changes. Current runner cases reconcile from 11 to 10; original review counts below remain historical. Other cleanup proposals, including task 18's duplicate smoke, remain outside this task.

B10 dry-run protection is now absent: deletion loses the partial check that dry-run adds no commit (and the dirty-status surrogate). Working-tree and index byte preservation were already unprotected. Passing remaining checks does **not** establish dry-run no-mutation. A focused byte/index-preservation regression remains separate opt-in work.

Verification: `python3 -B -m unittest scripts.test_run_codex_tasks` passed all 10 cases; `python3 -B scripts/test_run_codex_tasks.py` with each of `--smoke`, `--recovery-smoke` and `--resume-smoke` passed. Recovery deliberately failed its first fake review with exit 2, checkpointed and retried successfully. Checks used isolated temporary Git repositories and fake Codex only; all temporary roots and their parent were removed. `git diff --check` passed. Logs, original source, approved scope, mutation fingerprints and cleanup evidence are under `.test-artifacts/feedback-1-task-14/`. No browser, Node/audio suite, build, model/paid smoke or real Codex session ran; task 14 requires only runner checks and prohibits real Codex validation. No owner acceptance gate is required after implementation.

## Task 16 cleanup — 2026-09-10

The owner's request approved the exact nine deletions: seven frontend progress/elapsed-copy assertions and two bootstrap pnpm/usage-copy assertions. All three mixed groups survive with their original behavioral assertions and preconditions; no executable case was removed or added. Stage-message content and elapsed formatting are now explicitly unprotected. Historical inventory/counts below remain the original review baseline; other proposals are unchanged.

Build passed; frontend and standalone bootstrap passed. Two canonical concurrent runs each passed 33/34 Node cases and failed the unchanged trim assertion at `qa.test.mjs:40` (500 versus 400), preventing their Python step. QA alone passed 4/4; all 34 canonical Node cases passed with file concurrency set to 1; both Python signal cases passed separately. Default concurrent execution remains a known limitation, consistent with shared-resource contention but without a captured definitive cause. A scoped temporary progress-copy mutation passed all 7 frontend cases; the initial overbroad mutation also touched an unrelated acknowledgement and correctly failed its retained assertion. No repository production edits or replacement tests.

[Task 16](../16-test-editable-copy-assertions.md) records full limits and evidence under `.test-artifacts/feedback-1-task-16/`. Logged fixture roots and temporary diagnostic copies were removed; diff whitespace check passed. No browser/visual, live model, paid or human listening acceptance claimed or required.

## Readiness and execution

Existing Node/pnpm/TypeScript, Python signal environment, FFmpeg, Git, ps and lsof were available. Controlled subprocesses and temporary repos only; no model inference, Ollama request, paid API generation, dependency installation or real Codex task-runner session.

- `pnpm build`: passed ([log](build.log)).
- `node --test batches.test.mjs elevenlabs.test.mjs backend.test.mjs service.test.mjs qa.test.mjs prompt-plan.test.mjs review-store.test.mjs studio.test.mjs studio-frontend.test.mjs studio-sound-grouping.test.mjs quality-loop.test.mjs`: **26 passed**, 0 failed/skipped/todo, 53.1s ([log](node-baseline.log)). This is **not** the complete canonical package command.
- `.runtime/signal-venv/bin/python qa_test.py`: **2 passed** ([log](python-signal.log)).
- `python3 -m unittest scripts.test_run_codex_tasks`: **8 passed** ([log](python-runner.log)).
- `python3 scripts/test_run_codex_tasks.py --recovery-smoke` and `--resume-smoke`: both passed ([recovery](runner-recovery.log), [resume](runner-resume.log)).
- `standalone-core.test.mjs`: fully inspected, not executed. It calls `pnpm install --frozen-lockfile` twice in an isolated checkout. The review skill prohibits dependency installation during analysis. No inherited pass is claimed; packaging/first-use execution remains unverified here.
- `scripts/studio-ui-browser-check.mjs`: fully inspected, not executed. Six obsolete scenarios conflict with current product behavior; running the existing script cannot establish a current passing browser workflow.
- `smoke.mjs`: inspected, not run because it performs real local generation and a paid recreation. `judge-smoke.mjs`: inspected, not run because its CLAP outcome is obsolete and it can invoke prompt planning first.
- `sound-cases.mjs` is a corpus-generation utility, not a test runner; inspected to classify its paid `--run` / inventory behavior. No disabled/quarantined Node/Python cases or hidden additional test files found in the source inventory. Browser fixture modes and `--crash`/`--fake-codex` modes are helpers, not extra tests.

The Node command ran every registered Node case but omits the standalone top-level script. Root package test command also omits runner Python tests and browser checks. Baseline source remained unchanged after build; only review artifacts were added. No discovery failure or unexpected runner warning appeared in the commands run. The recovery smoke deliberately emits a first-review process failure (exit 2), then passes only after the runner records its recovery checkpoint and completes the retry; this expected diagnostic is preserved in runner-recovery.log. Test discovery is based on explicit package enumeration, source inventory and native Node/unittest results; unrestricted `node --test` was avoided because helper modes and unrelated artifacts are not the declared suite.

## Units, decisions and counts

Executable case inventory: 26 named Node cases plus one top-level Node bootstrap script; 2 signal unittest cases; 8 runner unittest cases; 3 standalone fake-runner smoke functions; one imperative browser script; one real-generation smoke; one obsolete judge smoke. These are **43 executable entries**, not 70 independent runner tests. Helper/fixture entry modes are excluded. The quality denominator is **70 units**: the 26 named Node cases, 5 bootstrap groups, 21 browser groups, 3 real-smoke groups, 1 judge script, 11 runner cases/functions, and 3 signal groups. Heterogeneous imperative scripts are split by behavior, not individual assertions. `inventory.json` fixes every member and location before any proposed deletion.

Scope: core 57/70 (81.4%); supporting 12/70 (17.1%); incidental 1/70 (1.4%); unknown 0/70 (0%). Above relevance threshold: 63/70 (90%); six obsolete browser scenarios and obsolete judge execution are below it. Full retention standard by inspection: 57/70 (81.4%); mixed 4/70 (5.7%); bad 9/70 (12.9%); uncertain 0. A good inspection decision does not turn an unrun entry into demonstrated execution.

Actions reconcile to 70: keep 57; remove assertions in four retained entries; remove six in-script browser scenarios; remove three whole executable entries (judge script, dry-run unittest, duplicate standalone fake-runner smoke). Thus ten quality units contain proposed in-test/scenario deletions across four retained executable entries, and three entire entries are removal proposals. Browser scenario removals are six groups in one script, not six files. Confirmed defect categories: obsolete product protection (7 units), editable prose coupling (3 units), absent-value comparison (1 unit), ineffective central no-mutation oracle (1 unit), duplicate workflow (1 unit). No deletion quota or estimated timing/flake savings is claimed.

Only one selective diagnostic fault was needed: in a temporary copy, dry-run overwrote the tracked fixture with `DATA LOST`; the original test still passed. Control, fault and restored runs all passed ([evidence](dry-run-fault.log)); temporary copy was deleted and production files were never edited. This demonstrates failure for **that fault only**, not a suite-wide mutation score. Other judgments are supported by source/contract inspection and baseline, not fresh mutation evidence.

## Case decisions

All browser groups below were inspected but not run in this review; their obsolete scenarios prevent claiming a passing current browser workflow. Good unrun groups retain an inspection decision with moderate confidence; demonstrated obsolete contracts have high confidence. No historical flakiness inference is made.

| Test/group ID and location | Behavior / scope | Quality / action | Oracle, boundary and distinct protection (confidence: high for executed/source-demonstrated cases; moderate for good unrun bootstrap/browser/real-smoke units) |
|---|---|---|---|
| `backend/medium` — `backend.test.mjs:11` | B1,B4 / core | good / keep | Real child invocation records independent required flags/env; CPU fallback rejected; cancel, timeout and spawn failure exercised; actual PID disappearance asserted. This uniquely protects subprocess adapter wiring, not model quality. |
| `backend/download` — `backend.test.mjs:75` | B1 / core | good / keep | Real downloader with stub curl verifies continuation offset, exact bytes, one download across rerun, final and partial mismatches preserved. No real network. |
| `batches/winners` — `batches.test.mjs:20` | B8,B7 / core | good / keep | Real HTTP/persistence/QA with generated WAV fixture verifies duplicate requests, pause across restart, selected hashes, revisions, paid operation membership and immutable old URLs. Unique multi-sound orchestration coverage. |
| `batches/crash` — `batches.test.mjs:70` | B8,B4 / core | good / keep | Independent process exits 23 after accepted work; restart call counter stays zero until acknowledge; remaining queue proceeds. Tests queue control, not backend semantics. |
| `elevenlabs/adapter` — `elevenlabs.test.mjs:11` | B5 / core | good / keep | Injected HTTP checks literal prompt, request args and credential header; real FFmpeg; exact reattached bytes and one POST; uncertain failure remains one POST after reset; receipt excludes key. Does not verify live provider behavior or endpoint host beyond current URL query. |
| `elevenlabs/studio` — `elevenlabs.test.mjs:47` | B5,B6,B7 / core | good / keep | Actual provider adapter/FFmpeg/workflow with fake HTTP yields normalized WAV, provenance export, retained bytes and reopened job without a second POST. Distinct integration beyond adapter-only check. |
| `prompt/validation` — `prompt-plan.test.mjs:8` | B3 / core | good / keep | Real parser and request constructor; independent four-prompt fixture plus preserved original, invalid output/schema/prefix, HTTP error, real bounded abort signals. Fenced output tests parser acceptance; does not establish semantic fidelity of model rewrites. |
| `prompt/interrupted` — `prompt-plan.test.mjs:41` | B4 / core | good / keep | Reads actual pre-model checkpoint, restores it as a crash would, reopens jobs and confirms no planner/generator replay plus specific error. Distinct planning boundary. |
| `prompt/durations` — `prompt-plan.test.mjs:73` | B2,B5 / core | good / keep | Real workflow validation accepts local 60, rejects cloud 60 and local 61 before execution; direct provider rejection separately covers bypassing this caller. |
| `qa/derivatives` — `qa.test.mjs:11` | B6 / core | good / keep | HTTP plus real Python/FFmpeg checks known synthetic active intervals, peak targets, exact frame bounds and boundary PCM fade samples, repeat bytes, unchanged source and authentication. Independent numeric outcomes. |
| `qa/offline` — `qa.test.mjs:104` | B6,B7 / core | good / keep | Real CLI subprocess and relocated bundle: original bytes preserved, mutations/missing source and lineage rejected, source silence fails explicitly, live-service offline mutations blocked. Distinct portable/offline boundary. |
| `qa/recovery` — `qa.test.mjs:223` | B4,B6 / core | good / keep | Controlled FFmpeg absence leaves failed evidence; retry archives exact failed bytes/partial output; analysis refresh preserves old bundle; interrupted attempt archived; simulated clock checks idle renewal. Failure injection reaches actual QA subject. |
| `quality/five-takes` — `quality-loop.test.mjs:29` | B3,B7 / core | good / keep | Known silence then tone produces exact [3,2,1,1,1] attempts and outgoing prompts, unique seeds; saved exact trim selection/stale predecessor/export history; recreation keeps actual prompt once. Fake backend does not claim paid provider correctness. |
| `quality/stop` — `quality-loop.test.mjs:70` | B3,B4,B5 / core | good / keep | Four explicit risk partitions: local static retries 15, paid static once, operational error once, canceled attempt once; real scheduler and abort with expected terminal outcomes. |
| `quality/recover` — `quality-loop.test.mjs:88` | B4 / core | good / keep | Hard exits at published source and cut, reopens real store, fails if planner replays and verifies only remaining four generations. Two distinct crash checkpoints share same oracle. |
| `review-store/lifecycle` — `review-store.test.mjs:15` | B7,B11 / core | good / keep | Independent WAV/evidence fixtures exercise immutable retries/conflicts, concurrent writers (one succeeds), invalid trust inputs, publication crash retaining partials, byte corruption/symlink rejection, relocation/history. Legacy review/evaluation metadata checks protect compatibility; evaluationData eligibility has old explicit API contract, current external usage is absent (do not infer obsolete permission from that alone). |
| `service/lifecycle` — `service.test.mjs:9` | B2,B3,B4 / core | good / keep | Controlled generator release validates actual auth/body/range contracts, contention/conflict, saved bytes, lost-response replay count, drain on close and interrupted restart. Distinct manual service lifecycle beneath Studio. |
| `service/owner` — `service.test.mjs:129` | B4 / core | good / keep | Real child stays alive for wrong identity, is stopped for matching identity; actual process identity absent afterward. Protects unrelated processes. |
| `service/session` — `service.test.mjs:151` | B1,B4 / core | good / keep | Real disposable output removed only once listening; retained/cache sentinels survive; idle callback closes server. Purposeful 1.1s timer observation is not evidence of historical flakiness. |
| `service/startup` — `service.test.mjs:199` | B1,B4 / core | good / keep | Isolated CLI fails at occupied port and missing setup while old output/cache/retained bytes and unrelated live listener survive; real PID and response checks. |
| `studio/resume` — `studio.test.mjs:78` | B3,B4 / core | good / keep | Actual job storage checks occupied replacement conflict does not mark resume, new work blocked, reopen replacement same ID, duplicate replacement denied and exact execution count. |
| `studio/integration` — `studio.test.mjs:107` | B2,B4,B7 / core | good / keep | Real Studio/session/CSRF/traversal/range/export, controlled generation children, cancel and orphan rescue, hard crash/reopen after relocation and stubborn setup-process reap. Export lineage and preserved feedback checked. No browser DOM XSS assertion; HTTP success is not a UI test. |
| `frontend/action` — `studio-frontend.test.mjs:8` | B9 / core | good / keep | Executes actual action helper extracted into VM: queued user callback runs after held background action. Unique queue behavior; source slicing couples helper boundaries but no observed repeated brittleness. |
| `frontend/progress` — `studio-frontend.test.mjs:22` | B9 / core | mixed / remove assertions | Real renderer over minimal DOM records correctly disables/re-enables generation, progress and spinner visibility. Seven exact prose/elapsed-format assertions at lines 32,34,35,38,41,45,47 pin editable wording/punctuation; delete those assertions, preserve state checks. Stage-message coverage becomes explicitly absent. |
| `frontend/grouping` — `studio-frontend.test.mjs:53` | B9,B7 / core | good / keep | Real groupTakes with independent source/analysis/trim/independent-run fixtures verifies versions, exact preferred candidate, durable sound membership and orphan fallback. Distinct presentation transformation. |
| `sound-grouping/persistence` — `studio-sound-grouping.test.mjs:8` | B3,B7 / core | mixed / remove assertions | Actual persisted jobs/reopen verify linked membership, stable keys/conflicts and explicit seeds. Line 28 compares absent budget to absent budget after budgets were removed; delete this assertion (and obsolete fixture-only budget_started_at assignment at 16). Surviving membership persistence is distinct from UI grouping. |
| `standalone/install` — `standalone-core.test.mjs:21` | B1 / core | good / keep | Isolated copied project installs/builds and checks self-contained runtime paths/unchanged lockfile. Legit packaging boundary, not a constant echo. Inspected; not executed because skill forbids dependency installation during analysis. |
| `standalone/imports` — `standalone-core.test.mjs:42` | B1,B2 / core | good / keep | Permission-restricted imports and real schema loader validate independent valid/invalid inputs with no runtime side effect. Imported symbol assertions accompany useful isolation checks. |
| `standalone/bootstrap` — `standalone-core.test.mjs:59` | B1 / core | mixed / remove assertions | First-use launcher from another cwd creates local build/token only and retains lockfile. Remove exact pnpm prose match at 64; actual build/status/local-files checks earn retention. |
| `standalone/invalid-request` — `standalone-core.test.mjs:71` | B1,B2 / core | good / keep | Both missing-file and schema-invalid make paths must fail before setup/session output mutation; sentinel byte checks independent. |
| `standalone/invalid-command` — `standalone-core.test.mjs:86` | B1 / core | mixed / remove assertions | Invalid and removed commands fail with no output setup or permission error. Remove exact usage lead-in at 90; preserve status, rejected import and no-side-effect checks. |
| `browser/fixture-preflight` — `scripts/studio-ui-browser-check.mjs:6` | B9 / core | good / keep | Fixture-root guard and readiness fixture value protect execution against an unintended live workspace; setup boundary, not a product test. |
| `browser/identity` — `scripts/studio-ui-browser-check.mjs:15` | B9 / core | good / keep | Real loaded candidates distinguish separate generation/seed identities even for identical audio. Different from pure VM grouping. |
| `browser/polling` — `scripts/studio-ui-browser-check.mjs:18` | B9 / core | good / keep | Actual DOM player identity, note focus and draft trim preserved after refresh/blind change. |
| `browser/single-player` — `scripts/studio-ui-browser-check.mjs:20` | B9 / core | good / keep | Real media elements: starting B pauses A; unique browser media integration. |
| `browser/shared-drafts` — `scripts/studio-ui-browser-check.mjs:23` | B9 / core | good / keep | Actual note/blind inputs survive navigation between listen/compare. |
| `browser/comparison-reload` — `scripts/studio-ui-browser-check.mjs:25` | B9 / core | good / keep | Reload restores exact comparison candidate; A/B labels identify comparison slots. |
| `browser/history-back` — `scripts/studio-ui-browser-check.mjs:28` | B9 / core | good / keep | Browser back restores candidate, scroll and note; exact numbers are saved-state contract. |
| `browser/review-export` — `scripts/studio-ui-browser-check.mjs:30` | B9 / core | good / keep | Actual approval action updates human state and serves export; archive correctness separately covered HTTP tests, this protects UI wiring. |
| `browser/rejection` — `scripts/studio-ui-browser-check.mjs:32` | B9 / core | good / keep | Actual reject action persists human state without increasing jobs; prevents accidental regeneration. |
| `browser/trim` — `scripts/studio-ui-browser-check.mjs:34` | B9 / core | good / keep | Actual trim form preserves generation count/membership and starts new version unreviewed. |
| `browser/source` — `scripts/studio-ui-browser-check.mjs:36` | B9 / core | bad / remove scenario | Lines 36–37 require no Download for source-only candidate and Original only wording; studio.js:128 now always provides Download. Obsolete source-only product expectation. |
| `browser/injected-states` — `scripts/studio-ui-browser-check.mjs:38` | B9 / core | bad / remove scenario | Lines 38–39 manufacture old budget/exhausted top-level state and demand 20 minutes recovery; semantic budgets removed, current exhausted outcome is completed. Current renderer state checks retained in frontend test. |
| `browser/semantic-setup` — `scripts/studio-ui-browser-check.mjs:40` | B9 / core | bad / remove scenario | Lines 40–41 inject removed judge readiness and dereference removed local-setup/cancel-local DOM IDs. Not a supported readiness path. |
| `browser/disconnect` — `scripts/studio-ui-browser-check.mjs:42` | B9 / core | good / keep | Temporary fetch failure exercises actual refresh/action/reconnect without replacing player. Fault injection models a real network boundary. |
| `browser/library` — `scripts/studio-ui-browser-check.mjs:44` | B9 / core | good / keep | Real filter rendering and 30-long-caption overflow/empty cases; DOM injected data intentionally tests rendering, not API persistence. |
| `browser/invalid-submission` — `scripts/studio-ui-browser-check.mjs:46` | B9 / core | good / keep | Real invalid submit rejects, displays error and clears locally persisted pending key; not a vacuous negative. |
| `browser/pending-request` — `scripts/studio-ui-browser-check.mjs:47` | B9 / core | bad / remove scenario | Lines 47–52 assign duration=1 to select that only offers 5/10/20/30/60, so submission never reaches expected lost-response pending state. Remove obsolete scenario; lost-response browser recovery remains a gap. |
| `browser/cancel-request` — `scripts/studio-ui-browser-check.mjs:54` | B9 / core | bad / remove scenario | Lines 54–55 submit removed budget field; workflowInput rejects before accepted cancellation exists. Remove obsolete scenario; browser cancellation wiring remains a gap. |
| `browser/background-complete` — `scripts/studio-ui-browser-check.mjs:57` | B9 / core | good / keep | Actual background completion preserves library/search focus and signals new candidate; backend fake still exercises browser integration. |
| `browser/completion-reload` — `scripts/studio-ui-browser-check.mjs:60` | B9 / core | good / keep | Reload preserves unobtrusive ready notification and view; distinct persisted browser state. |
| `browser/evaluation` — `scripts/studio-ui-browser-check.mjs:62` | B9 / core | bad / remove scenario | Line 62 requires semantic evaluation export HTTP 200; studio.mjs explicitly returns 410 and README removes evaluation tools. Obsolete central outcome. |
| `smoke/preflight` — `smoke.mjs:13` | B1 / core | good / keep | Real required key/model readiness before expensive smoke; operational safety gate, no claim of sound correctness. Inspected only; expensive generation/paid submission not run. |
| `smoke/local-paid` — `smoke.mjs:21` | B3,B5 / core | good / keep | Real five local variants and single explicit recreation with selected actual prompt; independent expected count and successful job states, durable keys reuse results. Inspected only; expensive generation/paid submission not run. |
| `smoke/delivery` — `smoke.mjs:27` | B6,B7 / core | good / keep | Loads actual candidate assets, verifies lineage and expected -3dB peak, records unreviewed listening status; distinct real-backend boundary beyond synthetic tests. Inspected only; expensive generation/paid submission not run. |
| `judge-smoke` — `judge-smoke.mjs:1` | B11 / incidental | bad / remove test | Whole obsolete real-CLAP script requires candidate.evaluation.model/evidence at 25–32; current runWorkflow returns evaluation:null and CLAP execution is explicitly removed. It can invoke local planner before failing. Remove script; legacy stored metadata compatibility remains covered by review-store/lifecycle, not new CLAP execution. |
| `runner/DiscoveryTests.test_discovers_numeric_order_and_skips_completed` — `scripts/test_run_codex_tasks.py:197` | B10 / supporting | good / keep | Independent files prove actual numeric discovery and authoritative completed flags; stable task format. |
| `runner/DiscoveryTests.test_task_status_is_authoritative_when_overview_is_stale` — `scripts/test_run_codex_tasks.py:207` | B10 / supporting | good / keep | Conflicting overview/task fixture proves parser uses task authority. Does not cover active implementation-before-review checkpoint; root reproduction demonstrates that separate missing gate. |
| `runner/ResultAndRenderingTests.test_reviewer_can_edit_and_run_local_checks` — `scripts/test_run_codex_tasks.py:216` | B10 / supporting | good / keep | Real review command constructor passed to captured subprocess boundary; explicit review permission flag is the contract, not mocked-subject success. |
| `runner/ResultAndRenderingTests.test_extracts_only_complete_structured_status_messages` — `scripts/test_run_codex_tasks.py:234` | B10 / supporting | good / keep | Real event parser independently distinguishes complete structured result and ordinary message; supports runner activity reporting without mistaking prose for completion. |
| `runner/ResultAndRenderingTests.test_rejects_unexpected_structured_status` — `scripts/test_run_codex_tasks.py:245` | B10 / supporting | good / keep | Real result file with disallowed status raises specific RunnerError type. Minimal file isolates validation path; no competing missing-file or parse failure. |
| `runner/GitRecoveryTests.test_dry_run_reports_but_does_not_commit_dirty_tree` — `scripts/test_run_codex_tasks.py:254` | B10 / supporting | bad / remove test | Central no-mutation dry-run expectation uses only dirty-tree and commit-count surrogates. Isolated fault overwrote tracked.txt with DATA LOST and test still passed; control/fault/restored all exit 0. Remove test and record dry-run byte-preservation gap (partial no-commit detection is lost). |
| `runner/FailureTests.test_reviewer_can_fix_critical_findings_before_approval` — `scripts/test_run_codex_tasks.py:268` | B10 / supporting | good / keep | Fake implementation/review writes actual temporary repo; real run_task commits reviewer change and accepts approved result containing fixed findings. File bytes and final commit checked; distinct runner orchestration, not model correctness. |
| `runner/EndToEndFakeCodexTests.test_reviewed_task_is_committed_and_runner_advances` — `scripts/test_run_codex_tasks.py:304` | B10 / supporting | good / keep | Separate fake executable, real runner/stream/schema/Git commit; process exit, observed review result and final commit/clean state. Keep as least-cost canonical survivor of duplicate smoke() flow. |
| `runner/smoke()` — `scripts/test_run_codex_tasks.py:98` | B10 / supporting | bad / remove test | Duplicates EndToEndFakeCodexTests with same one-task fake subprocess/repo. Extra count/status checks overlap commit_task enforced task-status and Git change checks; remove standalone --smoke function/dispatch. Loses direct total-commit count assertion, not distinct workflow. |
| `runner/recovery_smoke()` — `scripts/test_run_codex_tasks.py:126` | B10 / supporting | good / keep | Real fake-Codex subprocess fails first review; expected ordered recovery+task commits and complete/clean state demonstrate retry path distinct from happy flow. Does not prove partial bytes survive. |
| `runner/resume_smoke()` — `scripts/test_run_codex_tasks.py:157` | B10 / supporting | good / keep | Preexisting checkpoint commit and partial file; real process must preserve ancestry, use attempt-2 review log and clear active state. Distinct restart entry; fake reviewer does not inspect recovered patch contents. |
| `signal/regions-rhythm` — `qa_test.py:10` | B6 / core | good / keep | Independent anti-phase stereo event and silence plus periodic/steady tone counterexamples exercise actual signal analysis; protects channel cancellation and rhythmic warning discrimination. |
| `signal/static` — `qa_test.py:30` | B6 / core | good / keep | Seeded broadband, silence, colored and modulated noise test positive and false-positive partitions for actual static heuristic. Does not prove semantic sound quality. |
| `signal/removed-clap` — `qa_test.py:41` | B11 / supporting | good / keep | Actual WAV/analyze rejects removed semantic request while same valid input remains signal-readable; distinct Python entry from TS migration validation. |

## Behavior heat map

`strong` means retained tests satisfy the five inspection gates at that boundary; it does not mean every fault, browser journey or live provider was executed. Layers with no independent need use n/a.

| Behavior / impact | Unit | Integration / contract | E2E / other | Retained protection / gap |
|---|---|---|---|---|
| B1 setup/runtime / high | strong: backend/download | strong: backend/medium, service/startup/session | unknown execution: standalone/*, smoke/preflight | Root found interrupted signal-environment setup can return success with broken existing interpreter; current tests cover cancellation/missing setup, not partially installed environment. |
| B2 local trust boundary / critical | strong: prompt/durations | strong: service/lifecycle, studio/integration | weak: old browser script; browser XSS not asserted | Session/CSRF/Origin/Host and path/hash checks tested through real HTTP; exhaustive malformed inputs, cookie expiry and live-browser injection are not demonstrated. |
| B3 durable generation / high | strong: prompt/validation | strong: quality/five-takes, quality/stop, studio/resume | weak: browser/pending-request obsolete; real smoke unrun | Planner JSON/schema and independent original-copy protection, not semantic fidelity of four rewrites. |
| B4 recovery / critical | strong: service/owner | strong: quality/recover, prompt/interrupted, batches/crash, qa/recovery, studio/integration | strong: controlled subprocess crashes | Actual crash checkpoints/PID reaping covered; not every filesystem write boundary. |
| B5 paid single recreation / critical | strong: elevenlabs/adapter | strong: elevenlabs/studio, quality/stop | unknown: smoke/local-paid not run | Receipt/no-retry/call-count protection strong synthetically; credentials/env precedence and all HTTP error/oversize response branches not independently tested. |
| B6 signal/cut / high | strong: signal/regions-rhythm/static | strong: qa/derivatives/recovery, elevenlabs/studio | unknown: real smoke delivery | Known PCM fade/peak/frame outcomes protected. Root verified explicit cut 40–50s is rejected by stale maximum=30 despite supported 60s local audio: manual long-clip cut boundary is absent. Numerical checks do not establish human listening acceptance. |
| B7 durable human decisions/export / critical | strong: frontend/grouping at transformation only | strong: review-store/lifecycle, batches/winners, quality/five-takes, qa/offline | weak: browser selected-take journey absent in old script | Root diagnostic found retaining `source.wav` of cut candidate emits `audio.wav` with mismatched cut-bound metadata; existing retained-cut tests do not cover that source path. |
| B8 agent queues / high | n/a | strong: batches/winners/crash | absent current browser batch hash-route journey | API proves pause/restart, revision, exact winners; UI batch selector/form path needs independent current browser acceptance. |
| B9 human UI / high | strong: frontend/action/grouping; mixed progress prose | strong by inspection only: 15 surviving browser groups | weak: script cannot currently complete | Current five-take selection, batch deep link, local 60s/paid limit and source-only download are not covered by a passing current browser runner. |
| B10 task runner / high | mixed: dry-run oracle bad; parser/result/command tests strong | strong: fake process commit, review-fix, recovery/resume smoke | absent: interrupted completed-task marker before review | Root demonstrated `run()` skips task marked complete before consulting unfinished active-task state, reports all done without review, leaves unreviewed changes. Existing tests never create that crash window. Dry-run byte integrity fault survives reviewed test. |
| B11 migration / normal | strong: signal/removed-clap | strong: quality/five-takes input rejection and review-store legacy evidence | bad: judge-smoke new CLAP execution | Preserve read/export compatibility; remove obsolete execution tests, not historical metadata tests. |

## Required follow-up versus optional coverage

The suite has substantial real behavior protection and passes its executed synthetic portion. It is **not** a cleaned suite, a complete browser acceptance pass, or proof the project has no defects. See sibling tasks for exact proposed removals and current-browser coverage restoration. Each confirmed production defect already has a separate root-authored task that should carry its smallest behavioral regression; do not manufacture an exhaustive per-function suite. Partial-install recovery, source-only retain and active-runner pre-review interruption are evidenced missing outcomes, not reasons to delete the otherwise useful integration tests.

No extra owner approval was requested to finish this review. The skill requires a concrete approved plan before future test edits; these tasks are reviewable proposals. Source-generation smoke, model-quality judgment, arbitrary mutation tooling and historical flakiness investigation were best effort/outside safe baseline and did not block this report.
